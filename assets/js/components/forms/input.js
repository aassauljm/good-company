"use strict";
import React from 'react';
import {Input} from 'react-bootstrap';

export default Input;
/*

export default class Input extends React.Component {
    render(){
        return <div className="form-group">
            <input type={this.props.type}  className={"form-control "+(this.props.bsStyle || '')} {...this.props} name={this.props.ref} label={this.props.label} value={this.props.value}/>
        </div>
    }

}

*/